from ..redis.client import RedisClient


class Client:
    def __init__(self, base_url, enable_caching=True):
        pass

# Client().request().param('type', 'restaurant').param('location', '9999').post()
